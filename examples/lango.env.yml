sections:
  - name: app
    items:
      - key: SERVER_PORT
        value: "8080"
      - key: SPRING_PROFILES_ACTIVE
        values:
          prod: prod
          dev: dev

  - name: environment
    items:
      - key: ENVIRONMENT
        values:
          prod: prod
          dev: dev

      - key: REDIS_HOST
        values:
          dev: host.docker.internal
