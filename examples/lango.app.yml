name: lango

image:
  repository: lango-service

service:
  internal_port: 8080

healthcheck:
  path: /lango/liveness

env:
  shared:
    SERVER_PORT: "8080"
    SPRING_PROFILES_ACTIVE: prod
  environments:
    prod:
      ENVIRONMENT: prod
